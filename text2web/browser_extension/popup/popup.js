function display(str) {
  document.getElementById('JargonPopup').textContent = str;
}

/**
 * Called when popup has loaded
 */
document.addEventListener('DOMContentLoaded', function() {
  //display("hello");
});
